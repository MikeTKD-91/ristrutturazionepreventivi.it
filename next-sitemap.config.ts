import type { IConfig } from 'next-sitemap';
const config: IConfig = {
  siteUrl: 'https://ristrutturazionepreventivi.it',
  generateRobotsTxt: true,
  changefreq: 'weekly',
  priority: 0.8
};
export default config;